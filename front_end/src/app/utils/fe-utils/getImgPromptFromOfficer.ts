import {IOfficer} from '../darkest-space/interfaces/IOfficer.interface';

export const getImgPromptFromOfficer = (officer: IOfficer): string => {
  let prompt = "age 18, teen, sexy, cute girl, illustration, military tunic, portrait, focused on face";

  return prompt;
}
